# Database connection string from Neon
# Get this from your Neon project dashboard: https://console.neon.tech
# Format: postgresql://[user]:[password]@[hostname]/[dbname]?sslmode=require
DATABASE_URL="postgresql://user:password@hostname/dbname?sslmode=require"

# Neon Auth URLs
# Get these from provisioning Neon Auth on your branch via the Neon console or MCP server
# Format: https://ep-xxx.neonauth.c-2.us-east-2.aws.neon.build/[dbname]/auth
#
# Server-side URL (private - only accessible on the server)
NEON_AUTH_BASE_URL="https://ep-xxx.neonauth.c-2.us-east-2.aws.neon.build/dbname/auth"
#
# Client-side URL (public - exposed to the browser)
# Must be prefixed with PUBLIC_ for SvelteKit to expose it to the client
PUBLIC_NEON_AUTH_URL="https://ep-xxx.neonauth.c-2.us-east-2.aws.neon.build/dbname/auth"
